table: doc_type_node_attributes

relations:
  doc_type_node:
    type: belongs_to
    model: DocTypeNode
    fk: doc_type_node_id

presets:
  item:
    fields:
      - source: id
        type: int
      - source: name
        type: string
      - source: value
        type: string
      - source: position
        type: int
  api_item:
    fields:
      - source: name
        type: string
      - source: value
        type: string
